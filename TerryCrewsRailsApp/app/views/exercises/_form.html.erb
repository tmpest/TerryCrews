<%= form_for(@exercise) do |f| %>
  <% if @exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise.errors.count, "error") %> prohibited this exercise from being saved:</h2>

      <ul>
      <% @exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sets_count %><br>
    <%= f.text_field :sets_count %>
  </div>
  <div class="field">
    <%= f.label :rep_goal %><br>
    <%= f.text_field :rep_goal %>
  </div>
  <div class="field">
    <%= f.label :rep_results %><br>
    <%= f.text_field :rep_results %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
